apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-pro
  labels:
    app: cloud-pro
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cloud-pro
  template:
    metadata:
      labels:
        app: cloud-pro
    spec:
      containers:
      - name: api
        image: cloud-pro:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:postgres@db:5432/app"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
